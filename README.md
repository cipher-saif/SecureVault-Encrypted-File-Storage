# SecureVault-Encrypted-File-Storage
AES-256-GCM encrypted file transfer &amp; storage system with HMAC-SHA256 integrity, PBKDF2 key derivation, chunked processing, resume support, REST API, CLI, and a high quality web dashboard. Zero-knowledge architecture — server never sees plaintext.
